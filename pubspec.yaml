name: developer_app_list
description: "Flutter package to fetch and list apps developed by a given developer from the App Store and Play Store."
version: 0.0.1
homepage: https://pub.dev/packages/developer_app_list

environment:
  sdk: '>=3.3.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^3.0.0